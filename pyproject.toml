[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.coverage.run]
branch = true
source = ["prompt_lab"]

[tool.coverage.report]
show_missing = true
exclude_also = [
  "def choose_template\\(",
  "def main\\(\\) -> None:",
  "if __name__ == .__main__.:",
]
